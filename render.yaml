services:
  - type: web
    name: myproject
    env: python
    plan: free  # Plano gratuito; ajuste se necessário
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn myproject.wsgi:application"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: myproject.settings
